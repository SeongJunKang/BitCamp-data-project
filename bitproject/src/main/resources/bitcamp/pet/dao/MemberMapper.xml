<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="bitcamp.pet.dao.MemberDao">
	<resultMap type="Member" id="MemberMap">
		<id column="MNO" property="no" />
		<result column="NAME" property="name" />
		<result column="PWD" property="pwd" />
	</resultMap>
	
	<insert id="insert" parameterType="Member">
		insert into MEMBER(NAME,EMAIL,PWD,TEL,GEN,BTH)
		values(#{name},#{email},#{pwd},#{tel},#{gen},#{bth})
	</insert>
	
	<update id="update" parameterType="Member">
		update MEMBER set
		NAME=#{name},
		PWD=#{pwd},
		TEL=#{tel},
		where MNO=#{no}
	</update>
	<delete id="delete" parameterType="int">
		delete from MEMBER
		where MNO=#{no}
	</delete>
	<select id="selectList" resultMap="MemberMap" parameterType="map">
		<include refid="select01"/>
		order by MNO DESC
    limit #{startIndex}, #{length}
	</select>
	<select id="selectOne" parameterType="map" resultMap="MemberMap">
	   <include refid="select01"/>
	   where
	   <if test="no != null">
	   MNO=#{no}
	   </if>
	   <if test="email != null">
	   EMAIL=#{email}
	   </if>
	</select>
  <select id="countAll" resultType="int">
    select count(*) from MEMBER
  </select>
  <select id="isMember" parameterType="map" resultType="int">
    select count(*) 
    from MEMBER
    where EMAIL=#{email} and PWD=#{pwd}
  </select>
</mapper>












